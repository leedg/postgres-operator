# kube-linter config — chart 의 production-grade security baseline.
# NOTE: lefthook 의 kube-linter hook 은 이 config 를 참조하지 않고
# 기본 규칙으로 실행한다. 이 파일은 향후 CI 통합 시 사용할 예정.
#
# beta 단계 — rules 점진 활성 중.
checks:
  doNotAutoAddDefaults: false
  include: []
  exclude: []
