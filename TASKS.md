# TASKS — postgres-operator

> 작업 ID: F=기능 / I=개선 / B=버그 / T=그 외. 부여 후 재사용 금지.
> 단계: 설계(10%) / 구현(60%) / 테스트(90%) / 완료(100%).

## 현재 Phase: **P1** (0.4.0 — single-shard production-ready)

목표: RFC 0001 의 새 CRD spec 위에 single-shard reconcile + HA + pgBackRest 통합 + chaos-mesh E2E. 6+개월.

P0 (0.3.0-alpha redesign reset) 는 완료 (commit df1f2e1). 이전 phase 작업 표는 §"이전 Phase 기록" 참조.

현재 달성 마일스톤: `0.3.0-alpha.4` Day-0 alpha-deployable 실제 argos 배포 완료. 이는 G0 진척이며 P1 전체 완료(HA/backup/PITR)는 아니다.
<!-- live-verified: 2026-05-09 -->

## 작업 표 (P1)

| ID    | 기능명 / 요약 | 단계 | 완성도 | 의존 | 영향 | 비고 |
|-------|---------------|------|--------|------|------|------|
| F01a  | RFC 0001 PostgresCluster CRD v2 schema 실장 (types + CEL + webhook + deepcopy) | 완료 | 100% | - | F01b,F02~F05 | 2026-05-03. v1alpha1 in-place 본문 교체. 6-필드 spec, status.shards[], 3개 CEL XValidation. webhook 도메인 검증 (matrix/autoSplit/backup). |
| F01b  | F01a stub 해소 — 새 ShardsSpec/RouterSpec 기반 reconcile 본체 + builders 재배선 + envtest 재작성 | 완료 | 100% | F01a | F02~F05 | 2026-05-03. ShardStatefulSet/Service/ConfigMapName 도입 + Worker*/Coordinator* 제거. SelectorLabels(cluster, role, ordinal). Condition 카탈로그 RFC 0001 §3.4 (Ready/Progressing/ShardsReady/RouterReady/BackupHealthy/AutoSplitEligible). reconcile 본체: shard 루프 + router 분기 + status 종합 + applyClusterConditions. envtest 2종 (single-shard + native multi-shard) + cascade_delete (OwnerReference 검증 ADR 0008). lint 0 / test 3 specs pass / validate helm lint --strict pass. |
| F02   | instance manager P2-T3+ — postgres 프로세스 supervise + promote/demote 실장 | 테스트 | 90% | F01b,T15 | F03 | 2026-05-03. **5-cycle deployable 진행 완료**: (1) sample CR RFC 0001 v2 schema 재작성 (2) Dockerfile.pg 신규 — instance binary + postgres 이미지 (3a) builders.go env injection + pg_hba.conf + readiness/liveness probes (3b) per-cluster ServiceAccount/Role/RoleBinding + initdb init container + serviceAccountName (4) /readyz 에 sup.IsReady(PG round-trip) 통합 (5) hack/smoke.sh kind 스모크 + docs/operator-guide/deployment.md 가이드. lint 0 / 모든 패키지 test PASS / make validate (kustomize+helm lint --strict) PASS. 후속 (F02 100%): kind 실측 / WAL lag 측정 / chaos-mesh failover RTO 검증. |
| T15   | election lease 명명 규약 shard ordinal 모델로 마이그레이션 (F02 진입 정리) | 완료 | 100% | F01b | F02 | 2026-05-03. PrimaryLeaseName 시그니처 (cluster, role, pool string) → (cluster, role string, shardOrdinal int32). cmd/instance/main.go: POSTGRES_POOL → POSTGRES_SHARD_ORDINAL. router 호출 시 panic 으로 lease 부재 타입 강제. lint 0 / election test PASS (cov 97.5%). |
| F03   | RFC 0003 election / fencing 인터페이스 위 실장 완성 | 구현 | 79% | F02 | F04 | 2026-05-12. T19 에서 Pod UID 기반 election identity, `ReleaseOnCancel=false`, restarted standby marker 재구성까지 실장. Ready 이후 primary failure 는 이제 `status.phase=Degraded` + `FailoverReady=False` + promotion candidate 메시지로 노출한다. controller-layer promotion execution 은 replica Pod `postgres` container exec (`standby.signal` 제거 → `pg_ctl promote` → `pg_is_in_recovery()` polling) + promoted `instance-status` annotation patch 까지 구현. existing PGDATA old-primary standby marker 는 current primary endpoint 가 자기 자신이 아닌 경우로 일반화했고, main container 도 same `PRIMARY_ENDPOINT` 를 받아 marker 발견 시 `pg_rewind --target-pgdata ... --source-server ...` 후 `standby.signal`/`primary_conninfo application_name=<pod>` 를 구성한다. `pg_rewind` source normal connection 을 위해 HBA postgres trust line 을 scram rule 앞에 배치했고, rewind 실패 시 dataDir quarantine + fresh `pg_basebackup` fallback + 실패 시 원본 restore 를 구현했다. rejoin 준비 실패는 종료 전 `instance-status` annotation 에 `RejoinPreparationFailed` reason/message 를 patch 하고 controller 가 `status.shards[].replicas[]` 로 전달한다. 추가로 CNPG형 `spec.postgresql.synchronous.{method,number,dataDurability}` + CEL `number<=shards.replicas`, `required/preferred` `synchronous_standby_names` 렌더링, ConfigMap hash 기반 StatefulSet rolling reconcile 을 구현했다. 남은 범위: live divergent WAL rewind drill, 동기 복제 commit/RPO drill, network partition/STONITH 계열 검증. |
| F04   | pgBackRest 통합 — backup controller (`internal/controller/backup/`) | 구현 | 60% | F01b | F05 | `BackupJob` phase 전이 + `ScheduledBackup` CRD/controller + `RestorePIT` 호출 경로 + `executionMode=job` runner Job lifecycle + pgBackRest command-runner plugin + K8s sidecar exec 경로 완료. 남은 범위: WAL-G/Barman plugin, PITR 복구 drill. |
| F05   | single-shard E2E 시나리오 재설계 — chaos-mesh primary kill → failover < 30s | 설계 | 15% | F01b,F02,F03,F04 | - | RFC 0001 spec 기준 envtest 신규 작성 포함. T19 로 PG18 primary Pod delete smoke 는 RTO 21s PASS. Helm metrics Service/ServiceMonitor/PrometheusRule + BackupJob phase metric + PostgresCluster replication lag bytes metric/alert + PgBouncer exporter CNPG metric prefix alert 렌더 검증 + Grafana dashboard ConfigMap 렌더 검증, `Pooler` CRD/ConfigMap/Deployment/Service + PgBouncer/exporter health probe + parameter allowlist + `pg_hba` file 렌더링 + HA topology/PDB + zero-unavailable rolling update + `deploymentStrategy`/`serviceAccountName` + 사용자 제공 TLS Secret 렌더링/fail-closed 검증 + status `backendTargets/configHash` + `type=ro` ready replica host list/fast retry + `spec.paused` PAUSE/RESUME + `pgbouncer.parameters` 패치 in-place SIGHUP reload + 2026-05-12 `SMOKE_POOLER=1` Pooler Service psql/PAUSE/RESUME/config reload kind smoke PASS. 남은 범위는 chaos-mesh primary kill/network partition 기반 E2E, live Prometheus scrape/Grafana import, pgBackRest restore drill 결합 검증. |
| T22   | CNPG Database/User CRD 1차 이식 — psql reconcile | 구현 | 92% | F02 | F05 | 2026-05-12. `PostgresDatabase` CRD + controller 추가. ready primary Pod 의 `postgres` 컨테이너에서 `psql` shell script 로 `CREATE/ALTER/DROP DATABASE`, tablespace, schema, extension, FDW, foreign server, database/schema privilege grant/revoke 를 적용하고 status `applied/observedGeneration/conditions` 기록. `databaseReclaimPolicy=delete` finalizer 는 CR 삭제 시 실제 database drop 후 제거하도록 구현. `PostgresUser` CRD/controller 는 role flags, membership GRANT/REVOKE, `connectionLimit`, `passwordSecretRef`, `disablePassword`, `validUntil` 을 ready primary `psql` 로 적용한다. password Secret username match, `passwordSecretRef`+`disablePassword` conflict fail-closed, referenced Secret update watch, `status.passwordSecretResourceVersion`, `PostgresCluster.status.managedRolesStatus.byStatus/cannotReconcile/passwordStatus` 집계 검증 완료. 남은 범위: live kind smoke, retain 정책 실측, actual password rotation SQL round-trip. |
| T23   | CNPG declarative hibernation annotation 호환 | 테스트 | 92% | F01b | F05 | 2026-05-12. `cnpg.io/hibernation=on/off` annotation 을 지원한다. `on` 이면 shard StatefulSet 과 PVC template 소유권을 유지한 채 `spec.replicas=0`, native router Deployment 도 `replicas=0`, `status.phase=Hibernated`, `status.conditions[type=cnpg.io/hibernation]=True` 로 수렴한다. `off` 또는 annotation 제거 시 desired replicas 로 재수화한다. envtest 로 hibernate/rehydrate 검증 완료. `SMOKE_HIBERNATION=1` 이 PVC marker row 생성 → hibernate → PVC 보존 확인 → rehydrate → marker `SELECT` 를 검증하도록 추가됐고 `hack/smoke_shell_test.sh` 로 helper 경로를 봉인했다. 남은 범위: kind live 실측. |
| T24   | CNPG ImageCatalog / ClusterImageCatalog 호환 | 구현 | 75% | F01b | F05,P9 | 2026-05-12. `ImageCatalog`/`ClusterImageCatalog` CRD 추가, `PostgresCluster.spec.imageCatalogRef.{apiGroup,kind,name,major}` 로 catalog image 를 선택한다. `apiGroup=postgresql.cnpg.io` 를 호환 입력으로 허용하고, catalog/major 누락이나 `postgresVersion` mismatch 는 fallback 없이 `ImageCatalogRejected` 로 fail-closed 한다. shard StatefulSet init/main container image 와 `postgres-image-catalog-sha256` rollout annotation 이 catalog entry 변경을 따라 갱신되며, catalog watch + envtest 로 검증 완료. 남은 범위: extension image volume mount, official digest catalog 공급, kind live rollout 실측. |
| T25   | CNPG standalone replica cluster / externalClusters streaming path | 구현 | 72% | F02,T24 | F05,P9 | 2026-05-12. `PostgresCluster.spec.externalClusters[].connectionParameters`, `password`, `sslKey`, `sslCert`, `sslRootCert`, `bootstrap.pg_basebackup.source`, `replica.enabled/source` 표면 추가. standalone replica 는 ordinal 0 도 external source 에서 `pg_basebackup` 을 수행하고 `standby.signal`/`primary_conninfo` 를 기록한다. `connectionParameters.{host,port,user,dbname,sslmode}` 를 `PRIMARY_CONNINFO` 로 연결했고, password Secret 은 `/tmp/primary.pgpass` passfile 로, TLS client key/cert/root cert 는 projected Secret → `/tmp/primary-client.*` 복사 후 `sslkey/sslcert/sslrootcert` 로 연결한다. instance manager 는 `POSTGRES_REPLICA_CLUSTER=standalone` 일 때 영구 follower election 을 사용해 local promotion 을 차단한다. source mismatch/missing host/incomplete SecretKeySelector 는 `ReplicaClusterRejected` 로 fail-closed 한다. envtest + unit test 완료. 남은 범위: WAL archive/object-store hybrid, distributed topology demotion/promotion token, live cross-cluster drill. |
| T16   | GitOps deploy 오버레이 도입 (mongodb-operator / valkey-operator 와 3-repo 정합) | 완료 | 100% | - | - | 2026-05-06. ADR-0006. `deploy/overlays/prod/{kustomization,delete-namespace}.yaml` + `deploy/postgres-cluster.yaml` (db ns) + `deploy/README.md`. `kustomize build deploy/overlays/prod` 렌더 PASS, Namespace 리소스 0 건. patch target name 은 namePrefix 적용 전 raw `system` (config/manager 직접 import). |
| T17   | PG17/PG18 Kind smoke matrix — runtime image + CR version + HA WAL override | 완료 | 100% | F02 | F02,F03 | 2026-05-07. `hack/smoke.sh` 가 `PG_MAJOR`/`POSTGRES_VERSION`/`NS`/`SHARD_REPLICAS` override 를 지원. PG17, PG18 각각 Kind 에서 image build/load → PostgresCluster Ready → `psql SELECT 1` PASS. `SHARD_REPLICAS=1` 에서 primary+standby 2 Pod, `pg_stat_replication WHERE state='streaming'` PASS(PG17/PG18). |
| T18   | failover smoke false-pass 제거 + PG18 차단 원인 실측 | 완료 | 100% | T17 | F03,F05 | 2026-05-07. `SMOKE_FAILOVER=1` 이 RTO 미측정/standby 미승격을 성공으로 통과하던 false-pass 제거. macOS `date -d` 의존 제거. 초기 실측은 FAIL 로 원인 확보: 같은 Pod name lease 재점유, `*-1` recovery 유지, PVC fence label 없음. |
| T19   | PG18 failover 차단 수정 + status 수렴 gate 추가 | 완료 | 100% | T18 | F03,F05 | 2026-05-07. election identity=`podName/podUID`, `ReleaseOnCancel=false`, `POSTGRES_MEMBER_COUNT`, restarted ordinal-0 standby marker/`primary_conninfo` 재구성, CR status polling 추가. PG18 `SHARD_REPLICAS=1 SMOKE_FAILOVER=1` smoke PASS: WAL streaming, RTO 21s(<30s), CR status primary=`quickstart18fo-shard-0-1`, restarted `*-0` standby 확인. |
| T20   | `make sbom` 타겟 추가 — release-smoke SBOM 단계 PASS 회복 (통합 plan T0-1 postgres 부분) | 완료 | 100% | -    | -    | 2026-05-07 it49 commit e0df946. valkey Makefile L465-472 syft 패턴 byte-identical 이식. v0.3.0-alpha.3 retroactive SBOM upload (gh release upload) 후 `release-smoke-test.sh v0.3.0-alpha.3` **12 PASS / 0 FAIL** 회복. SPDX-2.3, 811455 bytes, 90 packages. mongodb T22 (it48 e898c30) 의 cross-cut 진행. ~/.claude/plans/wondrous-tumbling-porcupine.md T0-1. |
| T21   | v0.3.0-alpha.4 release digest GitOps rollout + argos Day-0 배포 마일스톤 반영 | 완료 | 100% | T16,T20 | P1/G0 | 2026-05-08. GitHub Release/Pages chart/SBOM release-smoke 12 PASS / 0 FAIL. argos ArgoCD `platform-data-postgres-operator` Synced/Healthy(revision `cc662773f1a286d6b11a768af151db0ccd47b63f`), Deployment `1/1`, image `ghcr.io/keiailab/postgres-operator:0.3.0-alpha.4`, imageID `sha256:394ec5eb4aa09d316d957a3c751bb7283f21bfa71f19a9d2871ccbc7ec974f2f`, `PostgresCluster/argos-postgres` Ready=True. HA/backup/PITR 는 계속 열린 범위. |
| T26   | 0.3.0-alpha.18 cross-cut OSS 표준 부합성 강화 + OLM bundle 8 CRD 정합 | 완료 | 100% | T22,T24,T25 | P1/G0 | 2026-05-12. CNPG / community-operators OLM postgresql 표준 부합성 cross-validation 결과 발견된 gap 일괄 해소: (1) `moby/spdystream` v0.5.0→v0.5.1 (CVE-2026-35469 HIGH) 보안 fix, (2) OLM bundle 0.3.0-alpha.18 재생성 — 8 owned CRD + alm-examples 7 종 example 정합 (operator-sdk bundle validate `suite=operatorframework` 통과, 단 PDB policy/v1 false positive 2 건 잔존), (3) CHANGELOG 0.3.0-alpha.17/.18 항목 + SUPPORT.md 신설, (4) README 8 CRD 표면 표 + 빠른 시작 6 단계 워크플로 갱신, (5) `make validate` 게이트에 ① bundle CRD count ≥ 8 ② operator-sdk bundle validate (default + operatorframework suite) ③ Chart appVersion vs kustomize newTag vs dist image tag drift assertion ④ `.github/workflows/` 부재 (ADR-0009 enforce) 4 개 assertion 추가, (6) lefthook DCO/Conventional Commits 게이트 활성화 (`make hooks-install`/`hooks-check` wrapper + CONTRIBUTING 안내 정합). 잔여 범위: signed-off-by trailer 누락 commit 13 건의 retroactively 정정은 force-push 위험으로 보류, 다음 cycle 부터 모든 commit DCO compliant. |
| T27   | 신규 CRD live kind smoke 자동화 + Pooler built-in auth + password rotation — PostgresDatabase/PostgresUser/Pooler/ScheduledBackup/ImageCatalog | 구현 | 95% | T22,T24,T25,T26 | F05 | 2026-05-12. hack/smoke.sh 에 `SMOKE_DATABASE=1` (PostgresDatabase → status.applied + pg_database + reclaim=delete DROP), `SMOKE_USER=1` (PostgresUser → status.applied + pg_roles + DROP ROLE), `SMOKE_SCHEDULEDBACKUP=1` (cron → BackupJob 생성 검증), `SMOKE_IMAGECATALOG=1` (ImageCatalog + ClusterImageCatalog schema + lookup) 4 종 시나리오 추가. step number N/15 정합. `SMOKE_POOLER=1` 은 이전 cycle 에서 이미 구현. T27 ⑤ Pooler built-in auth — `spec.pgbouncer.authSecretRef` 미지정 시 operator 가 `keiailab_pooler_pgbouncer` LOGIN role + `<pooler>-builtin-auth` Secret 자동 생성 (CNPG `cnpg_pooler_pgbouncer` 호환). T27 ⑥ password rotation — annotation `postgres.keiailab.io/rotate-pooler-password=true` 적용 시 ALTER ROLE 새 password + Secret in-place update + status.builtinAuthLastRotation 기록. 3 신규 unit test (`TestPoolerBuiltinAuth_RequeuesWhenPrimaryNotReady`/`CreatesSecretAndExecutesRoleSQL`/`RotatesPasswordOnAnnotation`) 모두 PASS. PoolerReconciler.PodExecutor wiring 회귀도 동시 fix. 잔여 범위: live kind 실측 (5 종 × PG17/PG18 = 10 회 smoke — 본 cycle 에 PG18 진행 시작), ImageCatalog 가 STS image 에 실제 반영되는 회로 e2e 검증. |
| T28   | OperatorHub.io / community-operators GitHub PR 등록 절차 — 0.3.0-alpha.18 bundle | 설계 | 5% | T26 | P1/G0 | k8s-operatorhub/community-operators 에 keiailab/postgres-operator bundle PR. docs/operator-guide/community-operators-onboarding.md 작성, bundle 이미지 ghcr.io/keiailab/postgres-operator-bundle:0.3.0-alpha.18 push, CI 자동화. |
| T29   | Pooler TLS Secret 자동 발급 — cert-manager 통합 또는 self-signed CA path | 설계 | 0% | T27 | F05 | `spec.pgbouncer.clientTLSSecret`/`serverTLSSecret` 미지정 시 operator 가 cert-manager Certificate CR (Issuer/ClusterIssuer 참조) 또는 self-signed crypto/x509 mini-CA path 로 자동 발급. PgBouncer Deployment 가 자동 생성된 Secret 을 mount. CNPG 의 cert-manager 통합 패턴 호환. T27 ⑥ 마무리 후 진행. |

## 이전 Phase 기록 (P0 — 0.3.0-alpha redesign reset, commit df1f2e1)

T01 ~ T14 완료. 자체 분산 SQL 정체성 정착, ADR/RFC 0001~0005 작성, Citus 의존 코드 폐기, lint/test/validate PASS.

## 차단됨

(없음 — F01a 가 P1 진입 게이트였고 완료 상태. F01b 부터 순차 진행 가능.)

## 영향도 추적

F01a (CRD schema) 변경 시 → F01b reconcile 본체 + 모든 envtest 재검토.
F01b (reconcile 본체) 변경 시 → F02~F04 의 spec 인터페이스 점검.
F04 (pgBackRest) 변경 시 → BackupJob CRD `spec.tool="pgbackrest"` 통합 검증.
