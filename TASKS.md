# TASKS — postgres-operator

> Task ID: F = feature / I = improvement / B = bug / T = other. IDs are
> assigned once and never reused.
> Stages: Design (10%) / Implementation (60%) / Testing (90%) /
> Complete (100%).

## Current phase: **P1** (0.4.0 — single-shard production-ready)

Goal: on top of the RFC 0001 CRD spec, deliver single-shard reconcile + HA
+ pgBackRest integration + chaos-mesh E2E. 6+ months.

P0 (the 0.3.0-alpha redesign reset) is complete (commit df1f2e1). See
§"Previous phase record" for the older task table.

Current milestone: `0.3.0-alpha.4` Day-0 alpha-deployable in the real
argos cluster. That is a G0 milestone, not full P1 completion (HA / backup
/ PITR are still open).
<!-- live-verified: 2026-05-09 -->

## Task table (P1)

| ID    | Summary | Stage | % | Depends | Impacts | Notes |
|-------|---------|-------|---|---------|---------|-------|
| F01a  | RFC 0001 PostgresCluster CRD v2 schema (types + CEL + webhook + deepcopy) | Complete | 100% | - | F01b, F02–F05 | 2026-05-03. In-place rewrite of v1alpha1. 6-field spec, `status.shards[]`, 3 CEL XValidations. Webhook domain validation (matrix / autoSplit / backup). |
| F01b  | F01a stub resolution — reconcile body on the new `ShardsSpec` / `RouterSpec` + builders rewire + envtest rewrite | Complete | 100% | F01a | F02–F05 | 2026-05-03. Introduced `ShardStatefulSet`/`Service`/`ConfigMapName`; removed `Worker*` / `Coordinator*`. `SelectorLabels(cluster, role, ordinal)`. Condition catalog per RFC 0001 §3.4 (Ready / Progressing / ShardsReady / RouterReady / BackupHealthy / AutoSplitEligible). Reconcile body: shard loop + router branch + status aggregation + `applyClusterConditions`. envtest 2x (single-shard + native multi-shard) + cascade_delete (OwnerReference per ADR 0008). 0 lint / 3 spec PASS / `helm lint --strict` PASS. |
| F02   | Instance manager P2-T3+ — supervise the postgres process + promote / demote | Testing | 90% | F01b, T15 | F03 | 2026-05-03. **5-cycle deployable** done: (1) sample CR rewritten on RFC 0001 v2 schema; (2) new `Dockerfile.pg` (instance binary + postgres image); (3a) env injection + `pg_hba.conf` + readiness / liveness probes via `builders.go`; (3b) per-cluster ServiceAccount/Role/RoleBinding + initdb init container + `serviceAccountName`; (4) `/readyz` integrates `sup.IsReady` (PG round-trip); (5) `hack/smoke.sh` kind smoke + `docs/operator-guide/deployment.md` guide. 0 lint / all package tests PASS / `make validate` (kustomize + `helm lint --strict`) PASS. Remaining for F02 100%: kind measurement / WAL-lag measurement / chaos-mesh failover RTO verification. |
| T15   | Migrate the election lease naming convention to the shard-ordinal model (F02 entry cleanup) | Complete | 100% | F01b | F02 | 2026-05-03. `PrimaryLeaseName` signature `(cluster, role, pool string)` → `(cluster, role string, shardOrdinal int32)`. `cmd/instance/main.go`: `POSTGRES_POOL` → `POSTGRES_SHARD_ORDINAL`. Router call paths force lease-absence types with a panic. 0 lint / election tests PASS (cov 97.5%). |
| F03   | RFC 0003 election / fencing interface — full implementation | Implementation | 79% | F02 | F04 | 2026-05-12. T19 introduced Pod-UID-based election identity, `ReleaseOnCancel=false`, and restarted-standby marker reconstruction. Post-Ready primary failure is now surfaced as `status.phase=Degraded` + `FailoverReady=False` + promotion-candidate message. Controller-layer promotion executes via replica-Pod `postgres` container exec (remove `standby.signal` → `pg_ctl promote` → `pg_is_in_recovery()` polling) + promoted `instance-status` annotation patch. The existing-PGDATA old-primary `standby.signal` marker was generalized to the case where the current primary endpoint differs from this Pod, and the main container also takes the same `PRIMARY_ENDPOINT`, so when the marker is found it runs `pg_rewind --target-pgdata ... --source-server ...` and then writes `standby.signal` / `primary_conninfo application_name=<pod>`. To enable the `pg_rewind` source's normal connection, the HBA places a `postgres` trust line before the SCRAM rules; on rewind failure, the data dir is quarantined and a fresh `pg_basebackup` fallback runs (with original-data-dir restore on second failure). Rejoin-prep failure patches `RejoinPreparationFailed` reason/message into the `instance-status` annotation before exit; the controller surfaces it as `status.shards[].replicas[]`. We also implemented the CNPG-style `spec.postgresql.synchronous.{method,number,dataDurability}` + CEL `number<=shards.replicas`, `required/preferred` `synchronous_standby_names` rendering, and ConfigMap-hash-driven StatefulSet rolling reconcile. Remaining: live divergent-WAL rewind drill, synchronous-commit / RPO drill, network-partition / STONITH-class verification. |
| F04   | pgBackRest integration — backup controller (`internal/controller/backup/`) | Implementation | 60% | F01b | F05 | `BackupJob` phase transitions + `ScheduledBackup` CRD/controller + `RestorePIT` call path + `executionMode=job` runner Job lifecycle + pgBackRest command-runner plugin + K8s sidecar exec path all done. Remaining: WAL-G / Barman plugin, PITR restore drill. |
| F05   | Single-shard E2E scenario redesign — chaos-mesh primary kill → failover < 30 s | Design | 15% | F01b, F02, F03, F04 | - | Includes fresh envtest on the RFC 0001 spec. T19 made the PG18 primary-Pod-delete smoke PASS with RTO 21 s. Render-verified Helm metrics Service / ServiceMonitor / PrometheusRule + BackupJob phase metric + PostgresCluster replication-lag-bytes metric/alert + PgBouncer exporter CNPG metric-prefix alert + Grafana dashboard ConfigMap. `Pooler` CRD / ConfigMap / Deployment / Service + PgBouncer/exporter health probes + parameter allowlist + `pg_hba` file rendering + HA topology/PDB + zero-unavailable rolling update + `deploymentStrategy` / `serviceAccountName` + user-supplied TLS Secret rendering / fail-closed checks + status `backendTargets/configHash` + `type=ro` ready-replica host list / fast retry + `spec.paused` PAUSE/RESUME + `pgbouncer.parameters`-patched in-place SIGHUP reload + 2026-05-12 `SMOKE_POOLER=1` Pooler-Service psql / PAUSE / RESUME / config-reload kind smoke PASS. Remaining: chaos-mesh primary-kill / network-partition E2E, live Prometheus scrape / Grafana import, pgBackRest restore drill end-to-end verification. |
| T22   | First-pass CNPG Database/User CRD port — psql reconcile | Implementation | 92% | F02 | F05 | 2026-05-12. `PostgresDatabase` CRD + controller added. A `psql` shell script in the ready primary Pod's `postgres` container applies `CREATE/ALTER/DROP DATABASE`, tablespace, schema, extension, FDW, foreign server, and database/schema privilege grant/revoke; status `applied/observedGeneration/conditions` is recorded. The `databaseReclaimPolicy=delete` finalizer drops the database on CR deletion before removing itself. `PostgresUser` CRD/controller applies role flags, membership GRANT/REVOKE, `connectionLimit`, `passwordSecretRef`, `disablePassword`, and `validUntil` via ready-primary `psql`. Password Secret username match, `passwordSecretRef`+`disablePassword` fail-closed, referenced-Secret update watch, `status.passwordSecretResourceVersion`, and `PostgresCluster.status.managedRolesStatus.byStatus/cannotReconcile/passwordStatus` aggregation are all verified. Remaining: live kind smoke, retain-policy measurement, actual password-rotation SQL round-trip. |
| T23   | CNPG declarative-hibernation annotation compatibility | Testing | 92% | F01b | F05 | 2026-05-12. Supports `cnpg.io/hibernation=on/off`. With `on`, the shard StatefulSet and its PVC template stay owned by the operator but the database Pod `replicas=0`, the native router Deployment `replicas=0`, `status.phase=Hibernated`, and `status.conditions[type=cnpg.io/hibernation]=True`. With `off` (or annotation removed), the cluster rehydrates to the desired `replicas`. Hibernate / rehydrate verified via envtest. `SMOKE_HIBERNATION=1` adds a PVC-marker-row creation, then hibernate, PVC-preservation check, rehydrate, and a marker `SELECT`. `hack/smoke_shell_test.sh` pins the helper paths. Remaining: live kind verification. |
| T24   | CNPG ImageCatalog / ClusterImageCatalog compatibility | Implementation | 75% | F01b | F05, P9 | 2026-05-12. Added `ImageCatalog`/`ClusterImageCatalog` CRDs. `PostgresCluster.spec.imageCatalogRef.{apiGroup,kind,name,major}` selects the catalog image. `apiGroup=postgresql.cnpg.io` is accepted as a compatibility input; a missing catalog/major or a `postgresVersion` mismatch fails closed as `ImageCatalogRejected` (no fallback). Shard StatefulSet init/main container images and the `postgres-image-catalog-sha256` rollout annotation track changes to the catalog entry; catalog watch + envtest verified. Remaining: extension-image volume mount, official digest catalog supply, live kind rollout measurement. |
| T25   | CNPG standalone replica cluster / externalClusters streaming path | Implementation | 72% | F02, T24 | F05, P9 | 2026-05-12. `PostgresCluster.spec.externalClusters[].connectionParameters`, `password`, `sslKey`, `sslCert`, `sslRootCert`, `bootstrap.pg_basebackup.source`, and `replica.enabled/source` surface added. A standalone replica runs `pg_basebackup` from the external source even on ordinal 0 and writes `standby.signal` / `primary_conninfo`. `connectionParameters.{host,port,user,dbname,sslmode}` map to `PRIMARY_CONNINFO`; the password Secret becomes a `/tmp/primary.pgpass` passfile, and TLS client key/cert/root cert are projected via Secret → `/tmp/primary-client.*` and supplied as `sslkey/sslcert/sslrootcert`. The instance manager uses a persistent-follower election when `POSTGRES_REPLICA_CLUSTER=standalone`, blocking local promotion. Source mismatch / missing host / incomplete `SecretKeySelector` fail-closed as `ReplicaClusterRejected`. envtest + unit tests pass. Remaining: WAL-archive / object-store hybrid, distributed-topology demotion / promotion-token, live cross-cluster drill. |
| T16   | GitOps deploy overlay (3-repo alignment with mongodb-operator / valkey-operator) | Complete | 100% | - | - | 2026-05-06. ADR-0006. `deploy/overlays/prod/{kustomization,delete-namespace}.yaml` + `deploy/postgres-cluster.yaml` (db ns) + `deploy/README.md`. `kustomize build deploy/overlays/prod` render PASS; 0 Namespace resources. Patch target name is the raw `system` before the namePrefix is applied (config/manager direct import). |
| T17   | PG17/PG18 kind smoke matrix — runtime image + CR version + HA WAL override | Complete | 100% | F02 | F02, F03 | 2026-05-07. `hack/smoke.sh` accepts `PG_MAJOR` / `POSTGRES_VERSION` / `NS` / `SHARD_REPLICAS` overrides. PG17 and PG18 each: kind image build/load → `PostgresCluster` Ready → `psql SELECT 1` PASS. With `SHARD_REPLICAS=1`, primary + standby (2 Pods) and `pg_stat_replication WHERE state='streaming'` PASS on PG17 and PG18. |
| T18   | Eliminate failover-smoke false pass + measure the PG18 blocker | Complete | 100% | T17 | F03, F05 | 2026-05-07. Removed the `SMOKE_FAILOVER=1` false pass (RTO not measured, standby not promoted but the run reported success). Dropped the macOS `date -d` dependency. First real run failed and exposed the root causes: identical-Pod-name lease re-acquisition, `*-1` recovery persistence, and missing PVC fence label. |
| T19   | PG18 failover blocker fix + status-convergence gate | Complete | 100% | T18 | F03, F05 | 2026-05-07. Election identity = `podName/podUID`, `ReleaseOnCancel=false`, `POSTGRES_MEMBER_COUNT`, restarted-ordinal-0 standby marker / `primary_conninfo` reconstruction, and CR status polling added. PG18 `SHARD_REPLICAS=1 SMOKE_FAILOVER=1` smoke PASS: WAL streaming, RTO 21 s (< 30 s), CR status primary = `quickstart18fo-shard-0-1`, restarted `*-0` as standby. |
| T20   | Add `make sbom` target — restore the release-smoke SBOM step (cross-cut plan T0-1 postgres part) | Complete | 100% | - | - | 2026-05-07 it49 commit e0df946. Byte-identical port of the valkey `Makefile` L465-472 syft pattern. After a retroactive v0.3.0-alpha.3 SBOM upload via `gh release upload`, `release-smoke-test.sh v0.3.0-alpha.3` recovered to **12 PASS / 0 FAIL**. SPDX-2.3, 811455 bytes, 90 packages. Cross-cut with mongodb T22 (it48 e898c30). `~/.claude/plans/wondrous-tumbling-porcupine.md` T0-1. |
| T21   | v0.3.0-alpha.4 release digest GitOps rollout + argos Day-0 deployment milestone | Complete | 100% | T16, T20 | P1/G0 | 2026-05-08. GitHub Release / Pages chart / SBOM release-smoke 12 PASS / 0 FAIL. argos ArgoCD `platform-data-postgres-operator` `Synced/Healthy` (revision `cc662773f1a286d6b11a768af151db0ccd47b63f`), Deployment `1/1`, image `ghcr.io/keiailab/postgres-operator:0.3.0-alpha.4` (`sha256:394ec5eb4aa09d316d957a3c751bb7283f21bfa71f19a9d2871ccbc7ec974f2f`), `PostgresCluster/argos-postgres` `Ready=True`. HA / backup / PITR remain open. |
| T26   | 0.3.0-alpha.18 cross-cut OSS-standards hardening + 8-CRD OLM-bundle alignment | Complete | 100% | T22, T24, T25 | P1/G0 | 2026-05-12. Closed the gaps found by the CNPG / community-operators OLM postgresql cross-validation: (1) `moby/spdystream` v0.5.0 → v0.5.1 (CVE-2026-35469 HIGH) security fix; (2) regenerated the 0.3.0-alpha.18 OLM bundle — 8 owned CRDs + 7 alm-examples aligned (`operator-sdk bundle validate --select-optional suite=operatorframework` clean; 2 PDB policy/v1 false positives remain); (3) CHANGELOG 0.3.0-alpha.17/.18 entries + new SUPPORT.md; (4) README updated with the 8-CRD surface table + the 6-step quickstart; (5) added 4 `make validate` assertions: ① bundle CRD count ≥ 8, ② `operator-sdk bundle validate` (default + operatorframework suite), ③ Chart `appVersion` vs kustomize `newTag` vs dist image-tag drift assertion, ④ `.github/workflows/` absence (ADR-0009 enforce); (6) lefthook DCO / Conventional Commits gate active (`make hooks-install` / `make hooks-check` wrappers + CONTRIBUTING alignment). Remaining: 13 commits without sign-off trailers held back (force-push risk); from this cycle on every commit is DCO-compliant. |
| T27   | Live kind-smoke automation for the new CRDs + Pooler built-in auth + password rotation — PostgresDatabase / PostgresUser / Pooler / ScheduledBackup / ImageCatalog | Implementation | 98% | T22, T24, T25, T26 | F05 | 2026-05-12. Added 4 scenarios to `hack/smoke.sh`: `SMOKE_DATABASE=1` (PostgresDatabase → `status.applied` + `pg_database` + reclaim=delete DROP), `SMOKE_USER=1` (PostgresUser → `status.applied` + `pg_roles` + DROP ROLE), `SMOKE_SCHEDULEDBACKUP=1` (cron → BackupJob creation), `SMOKE_IMAGECATALOG=1` (ImageCatalog + ClusterImageCatalog schema + lookup). Step numbering N/15 aligned. `SMOKE_POOLER=1` already shipped in an earlier cycle. T27 ⑤ Pooler built-in auth — when `spec.pgbouncer.authSecretRef` is empty, the operator creates the `keiailab_pooler_pgbouncer` LOGIN role and the `<pooler>-builtin-auth` Secret (CNPG `cnpg_pooler_pgbouncer` compatible). T27 ⑥ password rotation — `postgres.keiailab.io/rotate-pooler-password=true` triggers a new `ALTER ROLE` password, in-place Secret update, and a `status.builtinAuthLastRotation` timestamp. **T27 ⑩** PostgresDatabase / PostgresUser `status.applied` non-convergence (PG18 kind iter#3) root-caused and fixed: (a) finalizer-add path now continues *in the same reconcile pass* instead of `Requeue:true` (eliminates informer-cache-stale loop on the unfinalized snapshot); (b) `statusUpdate` re-fetches the resource and retries once on `IsConflict` instead of silently dropping the status payload; (c) `logger.Info` added at the four decision points (finalizer-add / cluster-not-found / primary-not-ready / success) for next-iteration kind-drill diagnostics. The updated `TestPostgresDatabaseReconcileDeletePolicyAddsFinalizerBeforeApply` asserts single-pass `status.applied=true`; full `go test ./...` green. The `PoolerReconciler.PodExecutor` wiring regression was also fixed earlier in the same cycle. Remaining: live kind re-measurement (5 scenarios × PG17/PG18 = 10 runs — PG18 re-run executed after this fix); end-to-end verification that ImageCatalog actually rolls a new STS image. |
| T28   | OperatorHub.io / community-operators GitHub PR procedure — 0.3.0-alpha.18 bundle | Implementation | 60% | T26 | P1/G0 | 2026-05-12. `docs/operator-guide/community-operators-onboarding.md` drafted; OLM bundle package renamed `postgres-operator` → `keiailab-postgres-operator` to avoid the zalando namespace collision (commit 98bccc6). **Draft PR opened**: [k8s-operatorhub/community-operators#8109](https://github.com/k8s-operatorhub/community-operators/pull/8109) with `operators/keiailab-postgres-operator/0.3.0-alpha.18/{manifests,metadata,tests}` + `ci.yaml` (`updateGraph: replaces-mode`, `reviewers: eightynine01`). Remaining: maintainer-owned `ghcr.io/keiailab/postgres-operator-bundle:0.3.0-alpha.18` push (`write:packages` PAT required), community-operators CI green, flip the Draft PR to Ready-for-Review, maintainer merge → Artifact Hub OLM channel propagation. |
| T29   | Pooler TLS Secret auto-issuance — cert-manager `Certificate` CR + AutoTLS spec | Implementation | 70% | T27 | F05 | 2026-05-12. **Stage 1** added `spec.pgbouncer.autoTLS` (`issuerRef.{name,kind=Issuer\|ClusterIssuer}`, `serverEnabled`, `clientEnabled`, `commonName`, `dnsNames`) — CRD / bundle / dist all regenerated. **Stage 2** wired the controller: `ensurePoolerAutoTLS` upserts the cert-manager `Certificate` CR via `unstructured` (no SDK dep) and the Pooler Deployment mounts the auto-generated `tls.crt`/`tls.key`/`ca.crt`. New helpers `poolerEffectiveServer/ClientTLSSecretName` make user-supplied Secrets win when present. RBAC `cert-manager.io/certificates [get,list,watch,create,update,patch,delete]` added. 2 new unit tests (`TestPoolerAutoTLS_CreatesCertificate`, `TestPoolerAutoTLS_UserSuppliedSecretTakesPrecedence`) + sample `pooler_autotls.yaml`. Remaining: live cert-manager kind drill (Issuer provisioning → Secret ready → Pooler Pod reload), self-signed fallback when cert-manager is absent, automatic rotation observability. |

## Previous phase record (P0 — 0.3.0-alpha redesign reset, commit df1f2e1)

T01–T14 completed. Self-built distributed-SQL identity stabilized, ADR/RFC
0001–0005 drafted, Citus-dependent code removed, lint / test / validate PASS.

## Blocked

(None — F01a was the P1 entry gate and is complete. F01b onward proceeds
sequentially.)

## Impact tracking

When F01a (CRD schema) changes → F01b reconcile body + all envtests must
be re-reviewed.
When F01b (reconcile body) changes → the spec interfaces of F02–F04 must
be re-checked.
When F04 (pgBackRest) changes → re-verify the `BackupJob` CRD
`spec.tool="pgbackrest"` integration.
