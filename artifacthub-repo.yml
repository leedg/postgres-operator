# Artifact Hub repository metadata.
# repositoryID 는 Artifact Hub UI 에서 repository 등록 시 부여 — 등록 후 본 file 에 반영.
#
# Reference: https://artifacthub.io/docs/topics/repositories/

repositoryID: TBD-set-after-UI-registration

owners:
  - name: Keiailab
    email: support@keiailab.com

# Packages to ignore during scan.
# postgresql-operator (legacy name, v0.3.0-alpha.2 시점) — repo rename 후
# 'postgres-operator' 가 canonical chart. legacy entry 는 GH Pages index.yaml 의
# 보존된 history 로 인덱싱되지만 Artifact Hub 표시에서는 제외.
ignore:
  - name: postgresql-operator
    version: ".*"
