# Chart metadata for keiailab/postgres-operator (P1-4, ADR 0007).
# 본 chart는 operator deployment + RBAC + CRD + NetworkPolicy를 포함한다.
# PostgresCluster CR 인스턴스는 별도 chart(charts/postgrescluster, 후속) 또는
# kubectl apply -f examples/<sample>.yaml로 배포한다.
apiVersion: v2
name: postgres-operator
description: |
  Apache-2.0 PostgreSQL Kubernetes Operator. PGO-class operational quality is
  the target, but external operators/backends are not forked, embedded, or
  wrapped. Vanilla PostgreSQL 18+ baseline with a self-built distributed SQL
  roadmap (ADR 0001, ADR 0003).
type: application
kubeVersion: ">=1.26.0-0"

# Chart 자체 버전 — alpha 단계, breaking change 가능. SemVer 준수.
# alpha.N iterative pre-release 표기 (mongodb-operator beta.N 패턴 정합).
version: 0.3.0-alpha.7

# 본 chart가 packaging하는 operator 버전. cmd/main.go binary 버전과 일치.
appVersion: "0.3.0-alpha.7"

home: https://github.com/keiailab/postgres-operator
sources:
  - https://github.com/keiailab/postgres-operator
keywords:
  - postgresql
  - postgres
  - postgres-18
  - operator
  - kubernetes
  - keiailab
  - self-built-distributed-sql
  - apache-2.0
maintainers:
  - name: keiailab
    email: support@keiailab.com
    url: https://github.com/keiailab
annotations:
  # 본 chart의 alpha 신호. Helm hub/artifacthub에 alpha 라벨로 표시되도록.
  artifacthub.io/maturity: alpha
  artifacthub.io/license: Apache-2.0
  artifacthub.io/category: database
  artifacthub.io/operator: "true"
  artifacthub.io/operatorCapabilities: Basic Install
  artifacthub.io/prerelease: "true"
  artifacthub.io/links: |
    - name: Documentation
      url: https://github.com/keiailab/postgres-operator/blob/main/README.md
    - name: Source Code
      url: https://github.com/keiailab/postgres-operator
    - name: Release Process
      url: https://github.com/keiailab/postgres-operator/blob/main/docs/releases/release-process.md
  artifacthub.io/crds: |
    - kind: PostgresCluster
      version: v1alpha1
      name: postgresclusters.postgres.keiailab.io
      displayName: PostgresCluster
      description: coordinator, worker pool, stateless router 로 구성된 자체 분산 PostgreSQL 토폴로지를 선언한다 (vanilla PG18 default, ADR 0001 자체 분산 SQL).
    - kind: BackupJob
      version: v1alpha1
      name: backupjobs.postgres.keiailab.io
      displayName: BackupJob
      description: PostgresCluster 대상의 단일 백업 또는 복구 실행 요청을 선언한다
  artifacthub.io/crdsExamples: |
    - apiVersion: postgres.keiailab.io/v1alpha1
      kind: PostgresCluster
      metadata:
        name: quickstart
        namespace: default
      spec:
        deployment: development
        version:
          postgres: "18"
        coordinator:
          members: 1
          storage:
            size: 10Gi
        workers:
          - name: pool-a
            members: 1
            storage:
              size: 20Gi
        routers:
          replicas: 1
    - apiVersion: postgres.keiailab.io/v1alpha1
      kind: BackupJob
      metadata:
        name: quickstart-full-backup
        namespace: default
      spec:
        cluster:
          name: quickstart
        tool: pgbackrest
        repo: repo1
        type: full
        executionMode: job
  artifacthub.io/images: |
    - name: manager
      image: ghcr.io/keiailab/postgres-operator:0.3.0-alpha.4
  artifacthub.io/changes: |
    - kind: changed
      description: "0.3.0-alpha — 자체 분산 SQL keystone (ADR 0001). AGPL/BUSL 백엔드 의존 영구 제거 (ADR 0003). VersionSpec.Citus + ShardingSpec.Backend=citus 폐기 (BREAKING)."
    - kind: removed
      description: "internal/citus/ + internal/plugin/extension/citus/ 패키지 제거. CITUS_LIBPQ_DSN 환경 변수 폐기. citus extension Helm values 제거."
    - kind: added
      description: ADR 0001 (self-built distributed SQL keystone) + ADR 0002~0005 + RFC 0001~0005 (PostgresCluster v2 / ShardRange / ShardSplitJob / pg-router / distributed-tx).
