---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: imagecatalogs.postgres.keiailab.io
spec:
  group: postgres.keiailab.io
  names:
    categories:
    - postgres
    - image
    - all
    kind: ImageCatalog
    listKind: ImageCatalogList
    plural: imagecatalogs
    shortNames:
    - pgic
    singular: imagecatalog
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.images[*].major
      name: Images
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ImageCatalog is a namespace-scoped PostgreSQL image catalog.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ImageCatalogSpec is the spec shared by namespaced and cluster-wide
              catalogs.
            properties:
              images:
                description: Images is the list of images per PostgreSQL major version.
                items:
                  description: ImageCatalogEntry is the operand image for a single
                    PostgreSQL major version.
                  properties:
                    extensions:
                      description: |-
                        Extensions is the list of extension image candidates available with this image.
                        Only the storage and validation surfaces are provided; the actual
                        volume-extension mount path is wired up in a later phase.
                      items:
                        description: ImageCatalogExtension is the metadata for an
                          extension image attached to a catalog entry.
                        properties:
                          image:
                            description: Image is the extension image reference.
                            properties:
                              reference:
                                description: Reference is the OCI image reference.
                                minLength: 1
                                type: string
                            required:
                            - reference
                            type: object
                          name:
                            description: Name is the extension identifier.
                            minLength: 1
                            type: string
                        required:
                        - image
                        - name
                        type: object
                      type: array
                    image:
                      description: |-
                        Image is the immutable image reference used for this major version. Production catalogs
                        should pin by digest.
                      minLength: 1
                      type: string
                    major:
                      description: Major is the PostgreSQL major version. It must
                        be unique within the catalog.
                      format: int32
                      minimum: 1
                      type: integer
                  required:
                  - image
                  - major
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - major
                x-kubernetes-list-type: map
            required:
            - images
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
