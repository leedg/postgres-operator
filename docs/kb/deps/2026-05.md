# Dependency change log — 2026-05 (postgres-operator)

> Location standard: `<repo>/docs/kb/deps/YYYY-MM.md` (global `standards/enforcement.md §2.4`).
> Update policy: append to this file whenever a lockfile (`go.mod` / `go.sum`) diff occurs.

## Baseline (2026-05-06 — this file's seed)

- `go.mod` direct require: 8 entries
- `go.mod` total lines (direct + indirect): 91 entries
- Core direct deps:
  - controller-runtime / client-go (k8s.io)
  - lib/pq (postgres driver)
  - onsi/ginkgo + gomega (test)
  - prometheus/client_golang

At the seed time of this file, there are *no* outstanding dependency changes. On the next lockfile diff, append the changed items in tabular form.

## Change log (append-only)

| Date | Change | Reason | Verification (audit) |
|------|--------|--------|----------------------|
| 2026-05-06 | seed (no change) | introduce P1 traceability per enforcement §2.4 | n/a |
| 2026-05-09 | direct usage change (no require change) | audit RFC-0017 §3.4: when introducing the EventRecorder, `k8s.io/client-go/tools/record` is indirect → now used directly by the reconciler. No go.mod require change (already transitive) | go test -short ./internal/controller/... PASS 8.07s |

## Verification commands

```bash
git diff -- go.mod go.sum | head -100
make audit          # trivy fs (HIGH+CRITICAL, ignore-unfixed)
```

## Global references

- Enforced standard: `~/Documents/ai-dev/standards/enforcement.md §2.4`
- Pre-verification SDK: `sonatype-guide:sonatype-guide` — mandatory call before adding or upgrading dependencies.
